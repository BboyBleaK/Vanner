<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/imgicono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vanner"
        tools:targetApi="31">
        <activity
            android:name=".activities.EmpresaActivity"
            android:exported="false"
            android:label="@string/title_activity_empresa"
            android:theme="@style/Theme.Vanner.NoActionBar" />
        <activity
            android:name=".activities.ui.EmpresaActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CompanyActivity"
            android:exported="false" />
        <activity
            android:name=".activities.VistaPrueba"
            android:exported="false" />
        <activity
            android:name=".activities.PerfilUsuarioActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RegistroActivity"
            android:exported="false" />
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>